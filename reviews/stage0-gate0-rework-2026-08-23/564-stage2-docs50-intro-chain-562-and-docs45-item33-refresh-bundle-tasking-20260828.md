# 合刀：docs/50 intro 链尾 → 562 + docs/45 §3 第 33 项刷新 — 任务书

- 编号：`564-stage2-docs50-intro-chain-562-and-docs45-item33-refresh-bundle-tasking-20260828`
- 前置：`563` PASS；用户裁定：**合刀**
- 用户裁定：**C** + **D** + **合刀**；**O1=公开源 B 路**；post-(a) live refresh per `560`（**O1 仍 OPEN**）

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做（合刀 · 一步交卷） | **A.** `docs/50` §4.4 intro ⚠ 收据链尾 +1：`→ 556` 后续接 `→ 562`（第 33 项 post-(a) live refresh 证据里程碑 per `560`/`562`；链尾以 `562` 收口）；**B.** `docs/45` §3 O1 详细段刷新：第 33 项证据已文档化（per `560`/`562`）+ 文首/§1/§6.2/§7 四处同步；**C.** 可选 `docs/53` §5 第 33 项一句「intro 链尾 per `564`」；**D.** 回执 **仅 `564`** |
| 本刀不做 | 改 registry；实跑 `--live`；Gate/O1 PASS；宣布 O1 收口；拆多回执 |
| 禁止 | 删减 OPEN；谎称 mart/O1 已收口 |

## NOW

1. docs only（A+B+C 同 commit）
2. pack → 回执 **`564`**
3. **必须双推** → **`84` POLL**

## 红线

合刀单槽单回执；`560`/`562` ≠ O1 收口；O1 仍 OPEN 直至用户/Cursor 另裁 + 26X+。
