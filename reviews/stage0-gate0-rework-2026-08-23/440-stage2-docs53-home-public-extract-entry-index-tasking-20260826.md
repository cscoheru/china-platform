# docs/53 首页公开提取入口一览 — 缩刀任务书

- 编号：`440-stage2-docs53-home-public-extract-entry-index-tasking-20260826`
- 前置：`439` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/53` §5 增「首页公开提取入口一览」表（site-nav + `/public-extracts` + `#track-nbs-sample` + `#track-nbs-live` + `#overview` + `#track-hb`；链回执 `410`/`420`/`424`/`432`/`377`）；(2) 可选 `docs/45` §6.2 一句；(3) 非 O1/Gate PASS；(4) 回执 **`440`**（`-cc-`）|
| 本刀不做 | 改代码；Gate/O1 PASS |
| 禁止 | 删减 OPEN；谎称 O1 |

## NOW

1. docs only
2. pack → 回执 **`440`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS。
