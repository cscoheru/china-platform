# docs/50 §4.4 公网预览 URL 块补登 — 缩刀任务书

- 编号：`454-stage2-docs50-public-preview-url-block-tasking-20260827`
- 前置：`453` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**；preview 容器化 **择机**（本刀不做）

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/50` §4.4 **预览 URL** 块补登公网 URL（`https://china.3strategy.cc` + `/public-extracts` + 首页 4 deeplink 示例；链行 200 / `docs/53` §5 第 16 项 / 回执 `446`）；(2) 保留 localhost 段；(3) 非 O1/Gate PASS；(4) 回执 **`454`**（`-cc-`）|
| 本刀不做 | 改代码；Docker；Gate/O1 PASS |
| 禁止 | 删减 OPEN；谎称 O1 |

## NOW

1. 只改 `docs/50`
2. pack → 回执 **`454`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS。
