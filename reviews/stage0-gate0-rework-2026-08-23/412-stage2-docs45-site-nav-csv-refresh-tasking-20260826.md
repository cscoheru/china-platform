# docs/45 全站顶栏 + CSV 登记 — 缩刀任务书

- 编号：`412-stage2-docs45-site-nav-csv-refresh-tasking-20260826`
- 前置：`411` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/45` 登记全站 `site-nav` → `/public-extracts`（回执 `410`；smoke §13c）+ 四轨 CSV 静态下载（回执 `404`/`407`；`public/public-extracts/*.csv`）；(2) 可选 `docs/53` 一句；(3) 非 O1/Gate PASS；(4) 回执 **`413`**（`-cc-`）|
| 本刀不做 | 改代码；Gate/O1 PASS |
| 禁止 | 删减 OPEN；谎称 O1 |

## NOW

1. docs only
2. pack → 回执 **`413`**
3. 双推 → **`84` POLL**

## 红线

不 Gate/O1 PASS。
