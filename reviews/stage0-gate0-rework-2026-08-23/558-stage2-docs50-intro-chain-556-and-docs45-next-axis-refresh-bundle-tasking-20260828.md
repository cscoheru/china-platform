# 合刀：docs/50 intro 链尾 → 556 + docs/45 §3 下一轴刷新 — 任务书

- 编号：`558-stage2-docs50-intro-chain-556-and-docs45-next-axis-refresh-bundle-tasking-20260828`
- 前置：`557` PASS；用户裁定：**合刀**（不改协议；一把任务书多步、一个回执）
- 用户裁定：**C** + **D** + **合刀**；preview 容器化 **择机**；**O1=公开源 B 路**；SHA drift **(a) 已执行**（per `538`/`540`/`542`）

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做（合刀 · 一步交卷） | **A.** `docs/50` §4.4 intro ⚠ 收据链尾 +1：`→ 548` 后续接 `→ 556`（第 32 项下一轴里程碑 per `554`/`556`；链尾以 `556` 收口）；**B.** `docs/45` §3 O1 详细段刷新：第 32 项下一轴已文档化（per `554`/`556`）+ 文首/§1/§6.2/§7 四处同步；**C.** 可选 `docs/53` §5 第 32 项一句「intro 链尾 per `558`」；**D.** 非 O1/Gate PASS；**E.** 回执 **仅一个 `558`**（`-cc-`）|
| 本刀不做 | 改 registry；改代码；实跑 `--live`；Gate/O1 PASS；动里程碑表 21–32 行正文；拆成多回执 |
| 禁止 | 删减 OPEN；谎称 mart 真 SHA 已收口；交两个回执号 |

## NOW

1. docs only（A+B+C 同 commit）
2. pack → 回执 **`558`**（唯一）
3. **必须双推** → **`84` POLL**

## 红线

合刀仍是**单槽单回执**；下一轴登记是文档节点；O1 仍 OPEN 直至 mart 真 SHA + 26X+。
