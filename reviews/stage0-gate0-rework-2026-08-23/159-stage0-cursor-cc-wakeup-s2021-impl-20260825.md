# CC 唤醒催办 — S2.0.2.1 仍未交卷

- 编号：`159-stage0-cursor-cc-wakeup-s2021-impl-20260825`
- 日期：2026-08-25
- 对象：`queue_rev` 56 已发布约 8+ 分钟，无 `compute_file_sha` / 回执 `158`

## 要求

立即执行 **`157`**：落地 `scripts/compute_file_sha.py` + ≥5 pytest → 补 pack（含 docs/35）→ 回执 **`158`**。

禁止 idle 等聊天；见 `84` 心跳。

— End —
