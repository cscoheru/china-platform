# Stage 0 Gate 0 — Cursor 第三轮收口复核（R6 pack 同步后）

- 复核日期：2026-08-24
- 文件编号：`05-stage0-cursor复核3-20260824`
- 复验方：Cursor（架构/质量审计，只读）
- 上一轮：`reviews/stage0-gate0-rework-2026-08-23/04-stage0-cursor复核2-20260824.md`（R5 技术通过、pack/文档「全修」不通过）
- 本轮触发：用户确认复现后，对 R6 pack 同步结果做只读终验
- 方法：磁盘文件 + 本机命令；不改业务代码、schema、测试或 `evidence_pack/`

---

## 0. 评审输出（per PRD 16.3）

| 项 | 判定 |
|---|---|
| **结论** | **开发返工 R4+R5+R6（20/20）可收口；Stage 0 Gate 0 维持 BLOCKED** |
| Stage 0 Gate 0 | **BLOCKED**（仅 E-1 中文扫描 PDF + 用户政策 P-1/P-2/P-3） |
| R4+R5+R6 技术/文档/pack 返工 | **通过**（相对 `04` 未决项已全部关闭） |
| 进入 Stage 1 | **否** |
| 阻塞问题 | E-1；P-1/P-2/P-3（与 `04` §2 相同，未变） |
| 证据不足（非阻塞） | 本轮未独立重跑含 H-2 的全集 237（~6 min） |
| CC 待办 | **P0 提交**（见 §7）；可选 P1 全量 pytest 留档 |

**一句话：** `04` 留下的 pack 哈希漂移与 `docs/13` 自漂移已消除；开发侧可宣告返工闭环，但 Stage 0 仍因外部样本与政策阻塞，不得进入 Stage 1。

---

## 1. 本轮独立运行时证据

| 检查项 | 命令/方法 | 结果 | 对照 `04` |
|---|---|---|---|
| Pack 全量哈希 | SHA256 逐 artifact 对比 `manifest.json` | **0 mismatch / 428** | `04` 为 7/428 漂移 → **已修** |
| Pack 版本 | `manifest.json` `schema_version` | **1.1-R3G-R4** | 与 R4 口径一致 |
| `docs/13` 入 pack | manifest 含 `docs/13-r4-final-verification.md` | **已收录** | R6-E 自漂移根因文件已在包内 |
| Pytest collect | `python3 -m pytest --collect-only -q -p no:cacheprovider` | **237 collected** | 与 R5-D 公式一致 |
| Git 提交状态 | `git status` 关键交付物 | **未提交**（`??`） | P0 待 CC 执行 |

---

## 2. 阻塞问题（维持，与 R6 无关）

### 2.1 E-1 / B-01 中文扫描 PDF 缺失

- 严重性：P0
- 状态：**未变**
- 关闭条件：用户提供代表性中文扫描 PDF，或书面批准改变样本/门槛政策

### 2.2 用户政策 P-1 / P-2 / P-3

- 不降低 OCR 门槛（numeric ≥80%、char ≥90%、needs_review ≤30%）
- 不接受 1909 美国样本作为中国代表性来源（registry 有效 S3）
- Stage 0 维持 BLOCKED 直至 E-1 解除

---

## 3. 相对 `04` 的闭环对照

| `04` 未通过项 | 本轮判定 | 证据 |
|---|---|---|
| 入库 pack 7 文件哈希过期（R5-C） | **通过** | `pack_errors 0 of 428` |
| `docs/13` 编辑后未重建 pack（R6-E） | **通过** | manifest 与磁盘一致；`docs/13` 在 428 清单内 |
| 文档残留错数 24/47/425（R5-D/E） | **通过** | `docs/*.md` 中 47/425 仅出现在「X→Y」修正叙述，无活跃错误口径 |
| R5 技术 apply 链 / I-05 | **维持通过** | 未回退；`tests/conftest.py` 链式 apply 仍在工作区 |
| 全量 237 含 H-2 独立重跑 | **仍未做** | 非 Gate 阻塞；建议 CC 可选留档 |

---

## 4. 分层判定总表（终态）

| 层级 | 判定 |
|---|---|
| R4（6 项） | **通过** |
| R5（9 项） | **通过**（含 `04` 时未通过的 C/D/E） |
| R6（5 项，含 R6-E pack 同步） | **通过** |
| **开发返工合计 20/20** | **可收口** |
| Stage 0 Gate 0 | **BLOCKED**（E-1 + P-1/P-2/P-3） |
| 进入 Stage 1 | **否** |

---

## 5. CC 执行清单（审计员下发，非本角色执行）

### P0 — 提交（**必做**）

工作区关键文件仍为未跟踪状态。在确认 `pack_errors=0 of 428` 后：

```bash
git add docs/13-r4-final-verification.md evidence_pack/manifest.json \
  tests/conftest.py scripts/build_evidence_pack.py \
  # 以及本轮 R5/R6 涉及的其余已验收文件
git commit -m "$(cat <<'EOF'
chore(stage0): close R4-R6 dev rework; sync evidence pack to 428 artifacts

Pack hash verification 0/428; pytest collect 237. Stage 0 Gate 0
remains BLOCKED on E-1 (Chinese scanned PDF) per user policy P-1–P-3.
EOF
)"
```

### P1 — 全量 pytest 留档（**可选**）

```bash
# 干净 schema 后
python3 -m pytest -q -p no:cacheprovider 2>&1 | tee /tmp/stage0-pytest-237.log
# 期望：237 passed；记录 wall-clock 供 docs/13 §8 R6-A 交叉引用
```

### P2 — 变更后 pack 纪律（**强制**）

任何 artifact 变更后必须：

```bash
EVIDENCE_PACK_TEST_HOOKS=1 SKIP_PYTEST=1 SKIP_PSQL=1 python3 scripts/build_evidence_pack.py
# 验证：pack_errors=0 of 428
```

### P3 — 提交前 grep（**建议**）

确认无活跃错误数字（47、24/24、425）出现在非「修正叙述」上下文中。

### P4 — 停止声明（**强制**）

- Stage 0 **BLOCKED**（E-1 + P-1/P-2/P-3）
- 开发返工 **20/20 已闭环**
- **不得**自行进入 Stage 1

---

## 6. CC 红线（重申）

- 不得降低 OCR 门槛或将 spike 04 标为产品 PASS
- 不得将 1909 美国样本提升为 S0/中国代表性
- 不得批量爬取或构建官方记分卡
- 不得在未重建 pack 的情况下编辑 `docs/13` 或 pack 内 artifact
- 不得宣布 Stage 0 通过或启动 Stage 1

---

## 7. 三轮复核轨迹

| 轮次 | 文件 | 核心结论 |
|---|---|---|
| 1 | `03-stage0-cursor复验.md` | R4 未全过；apply 链断裂；pack R3 |
| 2 | `04-stage0-cursor复核2-20260824.md` | R5 技术过；pack/文档「全修」不过 |
| 3 | **本文件** | R4+R5+R6 可收口；Stage 0 仍 BLOCKED |

---

## 8. 本文件未做的事

- 未修改业务代码、schema、测试、`docs/00`–`docs/13` 或 `evidence_pack/manifest.json`
- 未执行 git commit（待 CC P0）
- 未重跑含 H-2 的全量 237 pytest
- 未将 Stage 0 BLOCKED 改写成 PASS

— End of Cursor R6 closure review (2026-08-24) —
