# 全站顶栏链到 /public-extracts — 缩刀任务书

- 编号：`409-stage2-layout-public-extracts-nav-tasking-20260826`
- 前置：`408` PASS（CSV 已登记）；CC 曾报无 §NOW
- 用户裁定：**C** + **D**

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `frontend/app/layout.tsx` 顶栏增全站常驻链 `/public-extracts`（四轨 demo / 非 O1）；(2) banner 补一句主演示入口；(3) ≥1 smoke/pytest；(4) 回执 **`410`**（`-cc-`）|
| 本刀不做 | 改 fixture；Gate/O1 PASS |
| 禁止 | 谎称四轨=O1 |

## NOW

1. layout 链 + 测
2. pack → 回执 **`410`**
3. **双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS。
