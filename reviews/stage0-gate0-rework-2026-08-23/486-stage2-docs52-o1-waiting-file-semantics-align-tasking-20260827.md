# docs/52 文首 O1/WAITING_FILE 语义对齐（per 484）— 缩刀任务书

- 编号：`486-stage2-docs52-o1-waiting-file-semantics-align-tasking-20260827`
- 前置：`485` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**；preview 容器化 **择机**；**O1=公开源 B 路（docs/52），不等用户投喂**

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/52` 文首 ⚠「O1 仍 OPEN（WAITING_FILE）」句改为与 `484` 一致：O1 仍 OPEN；主路径 = 本规划 B 路；`WAITING_FILE` = intake 出口码 / mart 真 SHA 未入仓语义，**非**「等用户投喂」；docs/51 A 路仍可用非唯一；(2) `docs/45` 刷新四处；(3) 可选 `docs/53` §5 第 21 项一句互链；(4) 非 O1/Gate PASS；(5) 回执 **`486`**（`-cc-`）|
| 本刀不做 | 改代码；live crawl；Gate/O1 PASS；删 OPEN |
| 禁止 | 谎称 O1 已收口；暗示必须用户投喂 |

## NOW

1. docs only
2. pack → 回执 **`486`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS；不等用户投喂。
