# docs/50 §3.3/§5.1 O1 表行 WAITING_FILE 语义对齐 — 缩刀任务书

- 编号：`490-stage2-docs50-o1-table-waiting-file-align-tasking-20260827`
- 前置：`489` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**；preview 容器化 **择机**；**O1=公开源 B 路（docs/52），不等用户投喂**

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/50` §3.3 表行（约行 118）+ §5.1 表行（约行 242）刷新：状态列保留 `WAITING_FILE` 为出口码/真 SHA 未入仓语义；收口路径列改为 **主路径 = docs/52 B 路**（试点轴 `NATIONAL_BULLETIN`），A 路 `--confirm-o1=PATH` 仍可用非唯一；(2) 可选文末/清单句（约 277/413/461）一句对齐；(3) `docs/45` 刷新四处；(4) 非 O1/Gate PASS；(5) 回执 **`490`**（`-cc-`）|
| 本刀不做 | 改代码；删 OPEN；Gate/O1 PASS；动 §4.4 里程碑表 |
| 禁止 | 谎称 O1 已收口；暗示必须用户投喂 |

## NOW

1. docs only
2. pack → 回执 **`490`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS；不等用户投喂。
