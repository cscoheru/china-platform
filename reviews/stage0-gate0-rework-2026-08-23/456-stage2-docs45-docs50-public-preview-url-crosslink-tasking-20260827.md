# docs/45 ↔ docs/50 §4.4 公网预览 URL 互链 — 缩刀任务书

- 编号：`456-stage2-docs45-docs50-public-preview-url-crosslink-tasking-20260827`
- 前置：`455` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**；preview 容器化 **择机**（本刀不做）

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/45` 刷新：文首 queue_rev 刷新行 + §1 + §6.2 + §7 互链 **`docs/50` §4.4 公网预览 URL 块**（回执 `454`；链行 200/`446`）；(2) 可选 `docs/53` 一句；(3) 非 O1/Gate PASS；(4) 回执 **`456`**（`-cc-`）|
| 本刀不做 | 改代码；Docker；Gate/O1 PASS |
| 禁止 | 删减 OPEN；谎称 O1 |

## NOW

1. docs only
2. pack → 回执 **`456`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS。
