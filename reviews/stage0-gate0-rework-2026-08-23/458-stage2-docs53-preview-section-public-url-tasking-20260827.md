# docs/53 §5 预览节公网 URL 首行补登 — 缩刀任务书

- 编号：`458-stage2-docs53-preview-section-public-url-tasking-20260827`
- 前置：`457` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**；preview 容器化 **择机**（本刀不做）

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/53` §5 预览节开头（`npm run dev` 块前或旁）补 1 行公网预览 URL（`https://china.3strategy.cc/public-extracts` + 首页 deeplink 提示；链第 16/18 项、`docs/50` §4.4、回执 `446`/`454`）；(2) 保留现有 localhost 说明；(3) 非 O1/Gate PASS；(4) 回执 **`458`**（`-cc-`）|
| 本刀不做 | 改代码；Docker；Gate/O1 PASS |
| 禁止 | 删减 OPEN；谎称 O1 |

## NOW

1. docs/53 only
2. pack → 回执 **`458`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS。
