# Stage 0 Gate 0 — Cursor 第二轮复核（对照 R5 返工）

- 复核日期：2026-08-24
- 文件编号：`04-stage0-cursor复核2-20260824`
- 复验方：Cursor
- 上一轮：`reviews/stage0-gate0-rework-2026-08-23/03-stage0-cursor复验.md`（2026-08-23，R4-6 独立复验）
- 本轮对象：Claude Code 声称 Cursor 复验发现已逐条修复（R5-A…R5-I）
- 方法：把 CC 声称当**声称**，把磁盘文件 + 本机命令当**证据**；不改仓库、不以叙事补齐缺口。
- 范围：Stage 0 Gate 0 / R5 针对 Cursor `03` 的返工闭环。不得据此进入 Stage 1。

CC 本轮声称：

- 真问题全修：R5-A 默认 apply 链 / R5-B builder 收集 / R5-C 重建 pack / R5-D 数字 / R5-E docs/11 清理 / R5-F docs/03 章节 / R5-G docs/09 R22-R26 / R5-H 相对路径 / R5-I 复跑证据
- 设计决策：`test_cleanliness.py` 3 处环境防御性 skip 是 R5-A `_db_reachable()` 风格，不算 skip-as-PASS
- 关键证明：237 = 31+21+20+30+29+18+11+17+39+21；Cursor `03` §9 三步全部独立复跑成功

---

## 0. 评审输出（per PRD 16.3）

| 项 | 判定 |
|---|---|
| 结论 | **R5 技术项通过；「文档与入库 pack 已全部对齐」不通过。** Stage 0 **维持 BLOCKED** |
| Stage 0 Gate 0 | **BLOCKED**（仅 E-1 中文扫描 PDF + 用户政策 P-1/P-2/P-3） |
| R5 技术返工（apply 链 / 治理可复现） | **通过** |
| R5「6/9 项全修 + pack 全量哈希仍有效」叙事 | **不通过** |
| 进入 Stage 1 | **否** |
| 阻塞问题 | 见 §2（仍仅为外部/政策；R5 未解开 E-1） |
| 重要但非阻塞 | 见 §3（pack 哈希漂移、文档残留错数） |
| 证据不足 | 见 §4（本轮未重跑含 H-2 的全集 237） |
| 要求 CC 修改的精确任务 | 见 §7（需用户另行下令） |
| 下一 Gate 进入条件 | 见 §8 |

**给评审的一句话：** R5 把 Cursor `03` 指出的真技术断裂（默认流程缺 002）修上了，可以关掉 `03` §3.2；不能按「全修、pack 425 全量哈希仍有效」验收。

---

## 1. 分层判定总表

| 层级 | 判定 |
|---|---|
| Cursor `03` 指出的 **apply 链 / 治理测试可复现**（R5-A） | **通过** |
| **builder 收录 `002.sql`**（R5-B） | **通过** |
| **数字 237 = 31+21+20+30+29+18+11+17+39+21**（R5-D 主体） | **通过** |
| **docs/03 §4 / §9.5**（R5-F） | **通过** |
| **docs/09 R22–R26**（R5-G） | **通过** |
| **eval_report 相对路径**（R5-H） | **通过** |
| cleanliness 3 处环境 skip 不算 skip-as-PASS | **接受**（与 `_db_reachable()` 同类；R22 已写明） |
| **入库 pack 与当前磁盘全量哈希一致**（R5-C 声称） | **不通过** |
| **docs 无残留错数**（R5-D/E 声称「全修」） | **不通过**（仍有 24/47/425/21 风险） |
| **本轮独立重跑全集 237**（R5-I 第三步） | **未做**（只独立做了 §9.B / §9.C / 默认 60） |
| **Stage 0 Gate 0** | **维持 BLOCKED** |
| **进入 Stage 1** | **否** |

---

## 2. 阻塞问题（与 R5 无关，维持）

### 2.1 E-1 / B-01 中文扫描 PDF 缺失

- 严重性：P0
- 状态：未变
- 证据仍为 `eval_report.json`：numeric 0.0% / char 3.7% / needs_review 450/450；`gate_thresholds.json` `gate_verdict=BLOCKED`
- 关闭条件：用户提供代表性中文扫描 PDF，或书面批准改变样本/门槛政策

### 2.2 用户政策 P-1 / P-2 / P-3

- 不降低 OCR 门槛
- 不接受 1909 美国样本代表中国（archive.org 有效等级 S3）
- Stage 0 维持 BLOCKED 直至 E-1 解除

R5 **不得**、也**没有**用这些政策换 Stage 0 通过。复验方同意维持。

---

## 3. 重要但非阻塞（R5「全修」叙事不成立的部分）

下列问题**不能**推翻 R5-A 技术通过，也**不能**解开 E-1，但足以判定 **R5-C / R5-D / R5-E 声称的「全修」不通过**。

### 3.1 入库 pack 已过期（R5-C）

仓库 `evidence_pack/manifest.json` 本轮独立检查：

| 项 | 结果 |
|---|---|
| `schema_version` | `1.1-R3G-R4` ✅（上一轮为 `1.1-R3G`） |
| `schema/migrations/002_source_governance.sql` | **在 pack 中**；磁盘 SHA `c41161a8…e168b` 与 manifest 一致 ✅ |
| `schema/01-core.sql` | SHA `09aa46f9…e089ea` 与 manifest 一致 ✅ |
| `db_ddl_execution.command` | `DROP+chain psql apply (R5-A)` ✅ |
| `applied_sqls` | `01-core.sql` + `002_source_governance.sql` ✅ |
| 禁止前缀 `/Users/` `/home/` `/tmp/` | 无 ✅ |
| wall-clock 字段 | 无 ✅ |
| manifest 自列 | 无 ✅ |
| 路径重复 | 0 ✅ |
| **`artifact_count`** | **427**，不是 `docs/13` §4 写的 **425** |
| **对当前磁盘独立复算哈希** | **7 个文件 size+hash 均不匹配** |
| `tests/conftest.py` | **不在 pack**（builder 只收 `tests/test_*.py`） |

哈希不一致的 7 个文件：

- `data/extracts/04-scanned-pdf/eval_report.json`
- `spikes/04-scanned-pdf/evaluate_04.py`
- `docs/03-source-registry.md`
- `docs/09-risk-register.md`
- `docs/11-stage0-review.md`
- `docs/12-stage0-closure-and-report.md`
- `docs/13-r4-final-verification.md`

典型顺序：先建 pack，再改文档，哈希作废。因此不能再写「425 artifacts 全量独立哈希 OK」。

相对上一轮 `03`：R3 快照（423 / `1.1-R3G` / 无 `002.sql`）**已替换为 R4 口径 pack**，R5-B 与 R5-C 的「收录 002、升级 schema_version」成立；R5-C 的「与当前磁盘逐项哈希一致」不成立。

### 3.2 文档残留错数（R5-D / R5-E 未扫干净）

| 位置 | 残留 | 磁盘/collect 真相 |
|---|---|---|
| `docs/13` §3、§6 | schema 负例仍写 **47** | collect **39** |
| `docs/13` §4 | pack 仍写 **425** | 入库 **427** |
| `docs/12` §7 | spike 00-national 仍写 **24 passed** | collect **31** |
| `docs/11` §1.3 表 | 仍写 **24/24** | collect **31** |
| `docs/11` §1.1、§10 | 风险仍写 **21 条 / R01–R21** | `docs/09` 已到 **R26** |
| `docs/12` I-04 | 仍写 **R13—R21** | 应为 R13—R26 或注明 R22–R26 另表 |

`docs/11` 文首 / §9.4 I-05 勾选 / §10「12 闭环 + 仅 B-01 部分」已对齐，这一块相对 `03` **算修好**。

`docs/13` §2 分套表已改为 31/39 且加总 237，与 collect 一致；同文件 §3/§6 未改干净。

---

## 4. 证据不足之处

- **全集 237 + H-2 worktree 哈希**：本轮（2026-08-24）**未**重跑含 `test_suite_leaves_no_worktree_trace_h2` 的整套。只确认 `pytest --collect-only` = 237，以及 §5 的 B/C/默认 60。
- `docs/13` §1.1 声称默认 pytest `237 passed in 342.06s`：本轮不独立背书该墙钟数字。
- `tests/conftest.py` 未进 evidence pack：未评估是否必须收录，仅记录缺口。

---

## 5. 本轮独立运行时证据（2026-08-24）

命令均在仓库根、PG17 `127.0.0.1:55440` / `cegr_test` 上执行。

### 5.1 R5-D 算术 — collect 237

```text
python3 -m pytest --collect-only -q -p no:cacheprovider
total 237
  31  spikes/00-national-yearbook-table/...
  21  spikes/00-provincial-yearbook-table/...
  20  spikes/01-national-yearbook/...
  30  spikes/02-provincial-yearbook/...
  29  spikes/03-municipal-bulletin/...
  18  spikes/04-scanned-pdf/...
  11  tests/test_cleanliness.py
  17  tests/test_evidence_builder.py
  39  tests/test_schema_negative.py
  21  tests/test_source_governance.py
sum_check 31+21+20+30+29+18+11+17+39+21 = 237
```

与 CC 声称的公式一致。上一轮 `03` 指出的「docs/13 表加总 238 / national 写成 24 / schema 写成 47」在 **§2 表** 已改；§3/§6 与 `docs/12` §7 仍残留。

### 5.2 Cursor `03` §9.B 等价 — 只 apply `01-core` 必红（R5-A 负例）

```text
DROP SCHEMA IF EXISTS cegr CASCADE
psql -f schema/01-core.sql
-- declared_source_level: 0 rows
STAGE0_SKIP_SCHEMA_APPLY=1 python3 -m pytest -q tests/test_source_governance.py
21 failed in 0.12s
```

与声称一致。证明：缺 002 时治理测试不能绿灯。

### 5.3 Cursor `03` §9.C 等价 — apply 002 后 60 绿

```text
psql -f schema/migrations/002_source_governance.sql
-- declared_source_level 存在；source_document_verification_event 存在
STAGE0_SKIP_SCHEMA_APPLY=1 python3 -m pytest -q \
  tests/test_source_governance.py tests/test_schema_negative.py
60 passed in 0.26s
```

与声称一致。

### 5.4 默认链（比 §9 第三步更短的等价证明）

```text
DROP SCHEMA IF EXISTS cegr CASCADE
# 不手动 apply
python3 -m pytest -q tests/test_source_governance.py tests/test_schema_negative.py
60 passed in 0.52s
-- 事后查询：declared_source_level 存在
```

`tests/conftest.py` session autouse 在 DB 可达时 `DROP` + 链式 apply `01-core.sql` + `migrations/*.sql`。默认流程不再依赖人工 apply 002。可关闭 `03` §3.2。

`scripts/build_evidence_pack.py` `run_db_apply()` 同样链式 apply，入库 pack 的 `applied_sqls` 与此一致。

### 5.5 R5-H 相对路径

`data/extracts/04-scanned-pdf/eval_report.json`：

- `truth_table`: `spikes/04-scanned-pdf/truth_p24.json`
- `extracted_file`: `data/extracts/04-scanned-pdf/extracted.json`

无 `/Users/`。`spikes/04-scanned-pdf/evaluate_04.py` 含 `_to_repo_relative()`。

### 5.6 skip 设计决策

`tests/test_cleanliness.py` 仍有 3 处 `pytest.skip`（无 `data/`、git 不可用）。本仓库默认有 `data/` 且为 git 库，正常复验路径不贡献 skipped>0。`docs/09` R22 已写明「环境防御性 skip 保留」。复验方**接受**，不计入 skip-as-PASS。

---

## 6. 对 R5-A…R5-I 的逐项复核

| ID | CC 声称 | 本轮判定 | 证据 |
|---|---|---|---|
| **R5-A** 默认 apply 链 | 全修 | **通过** | §5.2 负例 21 failed；§5.3 60 passed；§5.4 默认 60 passed + 列出现 |
| **R5-B** builder 收集 `002.sql` | 全修 | **通过** | glob `schema/migrations/*.sql`；classify `schema_migration_ddl`；pack 含该文件且 hash 匹配 |
| **R5-C** 重建 pack | 全修 | **部分通过** | 版本/002/链式 apply 已修；**当前磁盘 7 文件哈希不一致**；count 427≠425 |
| **R5-D** 数字 | 全修 | **部分通过** | collect 公式成立；`docs/13` §2 已改；§3/§6 与 `docs/12` §7 仍残留 47/24 |
| **R5-E** docs/11 清理 | 全修 | **部分通过** | 文首/§9.4/§10 I-05 与 237 已改；§1.3 仍 24/24；§1.1/§10 仍「21 风险」 |
| **R5-F** docs/03 章节 | 全修 | **通过** | §4.1–4.3 为 20/29/30；§4.4 为 0%/3.7%/100% BLOCKED；§9.5 澄清 CSV 无 `verification_status` |
| **R5-G** docs/09 R22–R26 | 全修 | **通过** | R22–R26 存在；`docs/12` §6 指向 R22–R25。I-04 行未写到 R26（次要） |
| **R5-H** 相对路径 | 全修 | **通过** | §5.5 |
| **R5-I** 复跑证据 | §9 三步全成功 | **B/C 独立确认；237 本轮未跑** | §5.2–5.4；§4 |

---

## 7. 要求 CC 修改的精确任务（需用户另行下令改仓库）

本文件为**复核意见**。下列任务**不是**本文件作者已实施的补丁：

1. 冻结文档后重建 `evidence_pack/manifest.json`，再对 **当前磁盘** 做一次独立逐项 SHA-256；更新 `docs/13` 的 `artifact_count` 为重建后的真实数字（当前磁盘对照入库为 427，重建后可能再变）。
2. 清掉残留错数：
   - `docs/13` §3、§6：47 → **39**
   - `docs/13` §4：425 → 与重建 pack 一致
   - `docs/12` §7：national **24** → **31**
   - `docs/11` §1.3：24/24 → **31/31**
   - `docs/11` §1.1、§10：21 风险 / R01–R21 → **含 R22–R26**
   - `docs/12` I-04：R13—R21 → 与 09 对齐
3. （可选）builder 收集 `tests/conftest.py`（R5-A 默认 apply 链的源文件目前不在 pack）。
4. **不要**降低 OCR 门槛，**不要**把 1909 美国样本标为代表性 S0，**不要**进入 Stage 1。

---

## 8. 下一 Gate 进入条件

相对 `03` §8，技术条件更新如下：

1. E-1 解除或用户书面批准缩小 Stage 0 验收范围（排除代表性 OCR 闭环）。**未变。**
2. ~~独立复验：只 `01-core` → 治理失败；apply 002 → 通过；builder/pytest 默认链式 apply。~~ **本轮已确认，可关闭。**
3. `pytest --collect-only` 与**所有**文档分套表一致且总和 237。**§2 表已一致；§3/§6/`docs/12` §7/`docs/11` §1.3 未一致。**
4. 入库 pack 的 `schema_version` 为 R4 口径且含 `002.sql`。**版本与收录已满足；与当前磁盘全量哈希未满足。**
5. `docs/11` 文首/附录与 §7 不再互相否定。**I-05/237 已满足；风险条数与 spike 00 测试数未满足。**

当前：**停止。等待审核。不得自行进入 Stage 1。**

建议评审口径：

- **R5 技术返工：通过**（可关闭 Cursor `03` §3.2）。
- **R5「全修」叙事：不通过。**
- **Stage 0：继续 BLOCKED。**

若只再收一轮小补丁：§7 的 1–2 项（重建 pack + 清残留数字）即可把 R5-C/D/E 从「部分通过」抬到「通过」；仍不能进入 Stage 1，除非处理 E-1。

---

## 9. 与上一轮 `03` 的对照

| `03` 发现 | R5 后本轮 |
|---|---|
| 默认 DB/builder 只 apply `01-core`，治理 21 failed | **已修**（conftest + builder 链式 apply；§5.4 证明） |
| pack `1.1-R3G` / 423 / 无 `002.sql` | **已修收录与版本**；**新问题**为 7 文件哈希过期 |
| collect 237 vs 文档 24/47/238 | **公式与 §2 表已修**；残留见 §3.2 |
| docs/11 I-05 部分完成 / 205 | **已修** |
| docs/03 §4.4 5/15 PASS | **已修** |
| docs/09 无 R4 风险 | **已修**（R22–R26） |
| eval_report 绝对路径 | **已修** |
| Stage 0 BLOCKED（E-1） | **维持** |

---

## 10. 本文件未做的事

- 未修改业务代码、schema、测试、`docs/00`–`docs/13` 或 `evidence_pack/manifest.json`。
- 未把 BLOCKED 改写成 PASS。
- 未重跑含 H-2 的全量 pytest。
- 未覆盖仓库 evidence pack。

— End of Cursor R5 second-round review (2026-08-24) —
