# CC 唤醒催办 — S2.3-lite idempotent 修复仍未交

- 编号：`209-stage0-cursor-cc-wakeup-s23-lite-fix-20260825`
- 日期：2026-08-25
- 对象：`queue_rev` 80（`207`）约 7+ 分钟，无修 / 回执 `208`

## 要求

立即执行 **`207`**：修 `test_migration_010_idempotent`（过滤纯注释/空语句）→ 全绿 → 回执 **`208`**。

禁止 idle；见 `84`。

— End —
