# docs/45 四轨 CSV 下载登记 — 缩刀任务书

- 编号：`406-stage2-docs45-csv-download-refresh-tasking-20260826`
- 前置：`405` PASS；CSV 下载已上线（`404`）
- 用户裁定：**C** + **D**；Cursor 代判

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/45` §1/§6.2 登记四轨 **CSV 静态下载**（`gen_public_extracts_csv.py` + `/public-extracts/*.csv`；回执 `404`；与 JSON 并列）；(2) 可选 `docs/53` 一句；(3) 显式非 O1/Gate PASS；(4) 回执 **`407`**（`-cc-`）|
| 本刀不做 | 改页面/CSV 字节；Gate/O1 PASS |
| 禁止 | 谎称 CSV=权威库 |

## NOW

1. docs only
2. pack → 回执 **`407`**
3. **双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS。
