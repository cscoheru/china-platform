# docs/45 §3 O1 OPEN 状态刷新（B 路主路径）— 缩刀任务书

- 编号：`484-stage2-docs45-o1-open-bpath-status-refresh-tasking-20260827`
- 前置：`483` PASS；POLL ~9m → 续刀
- 用户裁定：**C** + **D**；preview 容器化 **择机**；**O1=公开源 B 路（docs/52），不等用户投喂**

## SCHEMA / 裁定

| 决策点 | 裁定 |
|---|---|
| 本刀做 | (1) `docs/45` §3 O1 详细段 + §6.2 相关行刷新：明确 **O1 主路径 = docs/52 B 路**（试点轴 `NATIONAL_BULLETIN` per `480`/`482`）；`WAITING_FILE` 仅保留为 intake 出口码/mart 真 SHA 未入仓语义，**不再写成「等用户投喂才可继续」**；docs/51 用户投递仍可用但非唯一；(2) 文首刷新行 + §1 一句 + §7 pack 链；(3) 可选 `docs/50` §5 OPEN 必带一句同步；(4) 非 O1/Gate PASS；(5) 回执 **`484`**（`-cc-`）|
| 本刀不做 | 改代码；live crawl；启用 Hubei；Gate/O1 PASS；删 OPEN |
| 禁止 | 谎称 O1 已收口；暗示必须用户投喂 |

## NOW

1. docs only
2. pack → 回执 **`484`**
3. **必须双推** → **`84` POLL**

## 红线

不 Gate/O1 PASS；不等用户投喂。
