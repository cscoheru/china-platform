# 00-COMPASS — 会话常驻罗盘（唯一热记忆）

> **预算：全文 ≤ 80 行 / 6 KB。**

## 愿景（PRD）

可查询、可回溯研究底座。不以抓取页数为完成标准。

## 现行里程碑

| 项 | 值 |
|---|---|
| 计划 | `docs/54` |
| 阶段 | **M2**（M1 有限通过） |
| NOW | **635** M2-c+d+e 合刀 |
| KPI 现状 | 省级 COVERED **5/31**（M2-b） |
| 目标（本刀） | ≥20/31 + 核对表 + q1 页 |
| Gate | **未 PASS** |

## 红线

禁：目录/首页 FETCHED；补零；静默硬编码 value；自动 Gate/M2 PASS。

## NOW

635：扩省覆盖 → crosscheck → `/research/q1-2024-gdp`；集中摄影。见任务书 `635`。

## POINTERS

- `docs/56` · `docs/08b` §1.2  
- `633` 回执 / `634` 审计 / `635` 任务书  
- `00-DUAL-POLL-PROTOCOL` · `dual_poll_status.sh`

## 压缩后自检

阶段？NOW？禁做什么？
