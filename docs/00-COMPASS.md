# 00-COMPASS — 会话常驻罗盘（唯一热记忆）

> **预算：全文 ≤ 80 行 / 6 KB。超标先删再写。禁止把 docs/45、54、55、PRD、回执贴进本文件。**
> 新会话 / `/compact` 后：只读本文件。深读按 §POINTERS 按需打开，一次一个。
> 更新时机：NOW 任务切换、里程碑进出、用户新裁定。禁止每刀追加叙事。

## 愿景（PRD）

国家—省级—试点城市的**可查询、可回溯**研究底座。事实/派生/推断/判断分层。不建官员总分。不以抓取页数为完成标准。DSH 不作仓库。

## 现行里程碑

| 项 | 值 |
|---|---|
| 计划 | `docs/54`（U1–U5 已批） |
| 阶段 | **M2**（M1 **有限通过** 2026-08-31） |
| 拆分 | `docs/56`；M2-a（**631**）DELIVERED；等 M2-b |
| NOW | 等架构师签 **M2-b** 任务书 |
| 北极星 | 2024 年 31 省 GDP 一致率（08b） |
| 试点 | 苏深挖 + 粤浙（M3） |
| Gate | **未 PASS**。M1≠Gate1 全量；M2≠自动 PASS |

## 红线

禁：首页 HTML 当表源；PARTIAL 当完成；补零冒充覆盖；江苏页绑他省；LLM 改 value；自动 Gate/O1/M2 PASS。

KPI：`geo×indicator×year` 覆盖率 + `missing_reason`；核对一致率。禁：11/15、刀号。

## NOW（本阶段）

M2-a 已完成（`631-stage0-cc-m2-a-geo-inventory-receipt-20260831.md`，8 tests green）。  
等架构师签 M2-b：≥5 省 2024 GDP 表级源抓取 + SHA 锁 + observation 写入。

## POINTERS（冷）

- 里程碑：`docs/54` · M1：`docs/55` · M2：`docs/56`
- 研究问题：`docs/08b` §1.2
- 调度：`reviews/…/00-EXEC-QUEUE.md`（§CURRENT/§NOW）
- 冻结：`00-CC-CURRENT.md`（rev 320）

## 压缩后自检

阶段？NOW？禁做什么？细节再 Read **一个** pointer。
